# ── 工作区全量入库（含 source/ 源文件；无版权类排除）并默认作为私有仓库上传。
#    以下仅按「体积/可重建/运行时」排除；转公开需用户显式声明。

# ── 成品与派生二进制（体积大、走 GitHub Releases / git-lfs，不入库）──────────
output/
glossary.db
glossary.db-wal
glossary.db-shm

# ── 处理源文件产生的页图（体积大、可由源文件重建；文本中间产物如 PDF→HTML 保留入库）──
structured/raw/*.png
structured/raw/*.jpg
structured/raw/*.jpeg
structured/raw/*.webp
structured/raw/*.tif
structured/raw/*.tiff

# ── 系统与临时 ────────────────────────────────────────────────────────
*.tmp
.DS_Store
__pycache__/

# ── 运行时锁文件（store 状态锁/账本锁；本地运行产物，不入库）─────────
.state.lock
.events.lock
.usage.lock
.progress.lock
