# raw/ —— 处理源文件的中间产物（持久化供审查）

存放把源文件转成 `structured/` 文本的过程中产生的原始中间产物，**持久化保存**、便于后续审查与回溯：

- OCR 页图：`page-001.png` 等（体积大，已在 `.gitignore` 排除，本地保留）
- PDF→HTML：`book.html` 或按页 `page-001.html`（文本，随 git 提交）
- 每页 OCR 文本：`page-001.txt`（文本，随 git 提交）

文本中间产物可入库；二进制页图默认不入库（可用 git-lfs 或仅本地保留），都可由源文件确定性重建。
