# WIP：后处理规范 P0 实现（2026-09-03 交接）

> **临时文档**：记录今天的工作状态与待完成项，供明天接续。完成 P0 后可删除。
> 对应计划：`docs/postprocessing-spec.md`（P0 清单）与 `docs/epub-template-spec.md`。

## 1. 本轮目标

按 `docs/postprocessing-spec.md` 实现 P0（结构/溯源/目录层级）+ 样式瘦身 + 脚注语义化。
规范文档已写好（`docs/epub-template-spec.md` + `docs/postprocessing-spec.md`，AGENTS.md 已加引用）。

## 2. 已完成 ✅（代码已改，未提交）

| 项 | 文件 | 验证 |
|---|---|---|
| 目录层级链路：`rebuild_structure` 写 entry["level"] → `set_units` 持久化 → `structure_entries` 回填 | `structure/rebuild.py`、`store.py`、`orchestrator.py` | 逻辑闭合 |
| 目录层级渲染：嵌套 nav `<ol>` + 嵌套 NCX `<navPoint>` + `dtb:depth`；辅助 `_toc_depths`/`_toc_tree`/`_render_nav_items`/`_render_ncx_points` | `build/__init__.py` | 待回归测试 |
| 样式瘦身：`_STYLE_CSS` 只留功能性（img 限宽居中、p.imgp、section.footnotes），删 font-family/color/font-size/line-height/text-indent/justify | `build/__init__.py` | `test_build_epub_embeds_stylesheet` 已改写为瘦身回归测试，通过 |
| 脚注语义化：`FootnoteState` 跨单元全局编号 + `[^label]`→noteref + 定义块→aside(footnote) + 双向跳转 + `_page` 加 `xmlns:epub` | `build/html.py` + `orchestrator.py` `_render_and_pack` | 冒烟通过（`/tmp/opencode/fn_smoke.py`） |

验证基线：`uv run pytest -q tests/test_build.py` → **19 passed**（含改写后的样式断言）。

## 3. 当前工作区状态（未提交）

```
 M AGENTS.md                        # 产物规范引用（agent-vs-code/epub-template/postprocessing）
 M src/auto_common/workspace/store.py
 M src/auto_epublizer/build/__init__.py
 M src/auto_epublizer/build/html.py
 M src/auto_epublizer/orchestrator.py
 M src/auto_epublizer/structure/rebuild.py
 M tests/test_build.py
?? docs/agent-vs-code.md             # 上轮产物（未提交）
?? docs/epub-template-spec.md
?? docs/postprocessing-spec.md
```

## 4. 待完成（明天继续）⚠️

按 todowrite 剩余项，顺序执行：

1. **todo 6 溯源审计模块**（`src/auto_epublizer/qa/provenance.py`，新文件）：
   - 三边对账：spine vs entries → `E_UNIT_MISSING`/`E_UNIT_ORDER`
   - 媒体溯源：译文 img refs vs 源文（`E_MEDIA_LOST`/`E_MEDIA_ORDER`）
   - 逐段覆盖率：structured 段落 vs align src 归一化子串 → `provenance_coverage`
   - 目录层级：解析 nav 嵌套深度 vs 源 level 序列 → `E_TOC_FLAT`（源有层级 nav 扁平）/`W_TOC_DEPTH`
   - 设计要点已想清：spine 从 OPF 解析（idref→manifest href）；expected=有 md 且非 `skip_empty_unit` 的条目；media 比较 basename（unquote 后）；coverage 无 align 时为 None（convert 路径）
2. **todo 3 目录层级审计**（并入 provenance.py，非 audit_epub——需对照源 level）
3. **todo 7 qa 接线**：`orchestrator.qa` 增 provenance 结果 + TOC 对账（facts `source.toc` vs 单元标题，`W_TOC_MISSING`）→ `generate_report` 增字段并扩展放行条件；`QaResult` 加 `provenance_coverage`/`units_missing`/`units_order_ok`/`media_lost`/`toc_missing`/`toc_flat`；失败 reason 新增 `provenance_incomplete`
4. **todo 8 源文勘误留痕**：`g0.py` 加 `detect_corrections(text)->list[(wrong,right)]`；translate 与 import 两路径写 align 行时 `note` 前缀 `corr:wrong→right`（注意 `apply_corrections` 目前无调用点，需从 g0.py 导出接线）
5. **todo 9 回归测试**：目录层级（h1/h2/h3 → nav 嵌套 + NCX 嵌套）、脚注（noteref/footnote/全局连续）、溯源审计（缺失/乱序/丢图 fixture）、放行条件；全量 `uv run pytest -q` + `ruff check` + `ruff format --check`
6. **todo 10 文档同步**：AGENTS.md/skills 契约核对（如 `qa` 输出字段变化则同步 workflow.md/qa.md）；`postprocessing-spec.md` §8 勾选 P0 完成；按惯例等用户「提交并上传」

## 5. 关键上下文（明天接续必读）

- **facts TOC 形状**：EPUB → `[{"title","href"}]`；PDF → `[{"level","title","page"}]`；`W_TOC_MISSING` 按 `entry.get("title")` 匹配
- **toc_depths 复用**：provenance 需与 build 同算法——建议在 `build/__init__.py` 加公共 `toc_depths(entries)`（包一层 `_toc_depths`），避免导入私有函数
- **放行条件**：`coverage is not None` 时才要求 ≈1.0；`units_missing==0`、`units_order_ok`、`media_lost==0`、`not toc_flat` 恒要求；convert 路径（无翻译）coverage 跳过但 units/media 仍生效（media 为 structured↔structured 恒等）
- **脚注作用域**：仅非双语路径（`render_document`）；双语 `render_bilingual_document` 不处理（P2）
- **PDF 目录层级**：单 unit 无 heading_level（依赖 agent 预处理切分），规范里已写明，本轮 CLI 只提供 level 传递机制
- **LSP 误报**：pydantic/httpx/rapidocr/typer import、`test_build.py:120` media_files 参数类型——均为解释器未指向 venv 的既有误报，以 `uv run` 为准
- 工作区 `.progress.json` 无；`publication.json` 状态机只经 CLI 推进，agent 不手工编辑

## 6. 验证命令

```bash
uv run pytest -q                # 全量
uv run ruff check .
uv run ruff format --check .
uv run pytest -q tests/test_build.py   # 快速回归（当前 19 passed）
```